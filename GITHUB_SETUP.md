# Настройка GitHub Actions для автоматического обновления данных

Этот проект использует GitHub Actions для автоматического парсинга и обновления данных без необходимости в отдельном сервере.

## 🚀 Быстрый старт

### 1. Создайте репозиторий на GitHub

Если у вас ещё нет репозитория:
1. Создайте новый репозиторий на GitHub
2. Загрузите код в репозиторий

### 2. Настройте GitHub Actions

GitHub Actions уже настроен в файле `.github/workflows/update-data.yml`. Он будет:
- Запускаться каждый час автоматически
- Парсить прогнозы и новости
- Сохранять данные в папку `data/`
- Коммитить изменения в репозиторий

### 3. Настройте переменные окружения в фронтенде

Создайте файл `.env` в папке `Sports Betting App Design/`:

```env
# Замените на ваш username и название репозитория
VITE_GITHUB_REPO=your-username/your-repo-name
VITE_GITHUB_BRANCH=main
```

**Пример:**
```env
VITE_GITHUB_REPO=john-doe/webappsport
VITE_GITHUB_BRANCH=main
```

### 4. Включите GitHub Actions

1. Перейдите в Settings → Actions → General
2. Убедитесь, что "Allow all actions and reusable workflows" включено
3. В разделе "Workflow permissions" выберите "Read and write permissions"

### 5. Первый запуск

После первого push в репозиторий:
1. GitHub Actions автоматически запустится
2. Проверьте статус в разделе "Actions" вашего репозитория
3. После успешного выполнения в папке `data/` появятся файлы:
   - `predictions.json` - прогнозы
   - `news.json` - новости
   - `metadata.json` - метаданные обновления

### 6. Ручной запуск

Вы можете запустить workflow вручную:
1. Перейдите в раздел "Actions"
2. Выберите "Update Sports Data"
3. Нажмите "Run workflow"

## 📁 Структура данных

После первого запуска в папке `data/` будут созданы:

```
data/
├── predictions.json  # Последние 10 прогнозов
├── news.json         # Последние 10 новостей
└── metadata.json     # Метаданные (время обновления, количество)
```

## 🔗 Использование данных в приложении

Приложение автоматически читает данные с GitHub через:
- `https://raw.githubusercontent.com/your-username/your-repo/main/data/predictions.json`
- `https://raw.githubusercontent.com/your-username/your-repo/main/data/news.json`

## ⚙️ Настройка расписания

По умолчанию парсинг запускается каждый час. Чтобы изменить расписание, отредактируйте `.github/workflows/update-data.yml`:

```yaml
schedule:
  - cron: '0 * * * *'  # Каждый час
  # - cron: '0 */2 * * *'  # Каждые 2 часа
  # - cron: '0 0 * * *'   # Раз в день в полночь
```

Формат cron: `минута час день месяц день_недели`

## 🔧 Локальная разработка

Для локальной разработки вы можете:

1. **Использовать локальный сервер** (как раньше):
   ```env
   VITE_API_URL=http://localhost:3001/api
   ```

2. **Использовать GitHub данные**:
   ```env
   VITE_GITHUB_REPO=your-username/your-repo-name
   ```

3. **Запустить парсинг локально**:
   ```bash
   cd server
   node scripts/parse-and-save-to-github.js
   ```

## 🐛 Решение проблем

### GitHub Actions не запускается

1. Проверьте, что файл `.github/workflows/update-data.yml` существует
2. Убедитесь, что Actions включены в настройках репозитория
3. Проверьте синтаксис YAML файла

### Данные не обновляются

1. Проверьте логи в разделе "Actions"
2. Убедитесь, что парсеры работают корректно
3. Проверьте, что файлы коммитятся в репозиторий

### Ошибки CORS при чтении данных

GitHub raw.githubusercontent.com не имеет проблем с CORS, но если возникают ошибки:
1. Убедитесь, что репозиторий публичный (или используйте GitHub Token)
2. Проверьте правильность URL в `.env`

## 📱 Использование в APK

После настройки GitHub Actions:

1. Убедитесь, что `.env` файл настроен с правильным `VITE_GITHUB_REPO`
2. Соберите приложение: `npm run build`
3. Создайте APK через Capacitor (см. инструкции по созданию APK)

APK будет автоматически читать данные с GitHub, без необходимости в отдельном сервере!

## 🎯 Преимущества

✅ **Бесплатно** - GitHub Actions и Pages бесплатны для публичных репозиториев  
✅ **Автоматически** - данные обновляются по расписанию  
✅ **Без сервера** - не нужен отдельный хостинг  
✅ **Надёжно** - GitHub обеспечивает высокую доступность  
✅ **Просто** - всё работает "из коробки"

