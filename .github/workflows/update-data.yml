name: Update Sports Data

on:
  schedule:
    # Запускается каждый час
    - cron: '0 * * * *'
  # Ручной запуск через GitHub UI
  workflow_dispatch:
  # Запуск при push в main (опционально)
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - '.github/workflows/update-data.yml'

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Нужно для коммита обратно в репозиторий
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json
      
      - name: Install dependencies
        run: |
          cd server
          npm ci
      
      - name: Parse and save data
        run: |
          cd server
          node scripts/parse-and-save-to-github.js
        env:
          # Увеличиваем таймаут для парсинга
          NODE_OPTIONS: '--max-old-space-size=4096'
      
      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/*.json
          
          # Проверяем, есть ли изменения
          if git diff --staged --quiet; then
            echo "Нет изменений для коммита"
          else
            git commit -m "🤖 Auto-update data: $(date +'%Y-%m-%d %H:%M:%S UTC')"
            git push
            echo "✅ Данные обновлены и отправлены в репозиторий"
          fi
      
      - name: Summary
        if: always()
        run: |
          echo "## 📊 Результат обновления данных" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Workflow выполнен" >> $GITHUB_STEP_SUMMARY
          echo "- 📁 Данные сохранены в папку `data/`" >> $GITHUB_STEP_SUMMARY
          echo "- 🌐 Доступны через GitHub Pages или raw.githubusercontent.com" >> $GITHUB_STEP_SUMMARY

